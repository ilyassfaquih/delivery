export interface MenuItemDTO {
    name: string;
    price: number;
    available: boolean;
}

export interface MenuItem {
    id: number;
    name: string;
    price: number;
    available: boolean;
}
